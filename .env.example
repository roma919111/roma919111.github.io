# OpenArt MCP authentication
# Obtain an OAuth/access token for https://mcp.openart.ai/mcp
OPENART_ACCESS_TOKEN=

# Optional endpoint override
# OPENART_MCP_URL=https://mcp.openart.ai/mcp

# When true (default if no token), failed/missing live calls return demo media
OPENART_DEMO_FALLBACK=true
